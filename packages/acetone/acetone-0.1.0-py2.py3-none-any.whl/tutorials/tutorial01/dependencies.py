from acetone import AcetoneContainer

# Creates a lightweight container (dict) on a module level. The name is up to you,
# it can be 'services_container', 'dependencies_container' or as here, 'dependencies'.
# Whatever fits your code.
dependencies = AcetoneContainer()
