.. highlight:: shell

============
Installation
============


Stable release
--------------

To install BuildChecker, run this command in your terminal:

.. code-block:: console

    $ pip install BuildChecker

This is the preferred method to install BuildChecker, as it will always install the most recent stable release. 

If you don't have `pip`_ installed, this `Python installation guide`_ can guide
you through the process.

.. _pip: https://pip.pypa.io
.. _Python installation guide: http://docs.python-guide.org/en/latest/starting/installation/


From sources
------------

The sources for BuildChecker can be downloaded from the `Github repo`_.

You can either clone the public repository:

.. code-block:: console

    $ git clone git://github.com/ashwin1992/BuildChecker

Or download the `tarball`_:

.. code-block:: console

    $ curl  -OL https://github.com/ashwin1992/BuildChecker/tarball/master

Once you have a copy of the source, you can install it with:

.. code-block:: console

    $ python setup.py install


.. _Github repo: https://github.com/ashwin1992/BuildChecker
.. _tarball: https://github.com/ashwin1992/BuildChecker/tarball/master
