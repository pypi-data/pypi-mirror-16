Getting started:
================

1. Install the required packages:

    $ sudo apt-get install bzr fabric firefox python-dev python-pip python-virtualenv xvfb

2. Get the code:

    $ bzr branch lp:u1-test-utils

3. Set up the environment:

    $ cd u1-test-utils
    $ fab bootstrap

4. Run the tests:

    $ fab test
