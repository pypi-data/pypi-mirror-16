.. _install:

============
Installation
============

You can call ISA by using `pip`:

.. code-block:: text

   pip install isa

Or you can install `isa` using `setup.py`:

.. code-block:: text

   python setup.py install