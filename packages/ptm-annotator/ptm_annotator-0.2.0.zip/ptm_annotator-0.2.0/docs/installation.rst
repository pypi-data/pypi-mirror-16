.. highlight:: shell

============
Installation
============

At the command line:

.. code-block:: console

    $ easy_install ptm-annotator

Or, if you have virtualenvwrapper installed:

.. code-block:: console

    $ mkvirtualenv ptm-annotator
    $ pip install ptm-annotator
