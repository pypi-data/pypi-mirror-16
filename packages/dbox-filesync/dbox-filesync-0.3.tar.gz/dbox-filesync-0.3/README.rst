DropboxFileSync
=======================

This package provides a class which encapsulates access to the Dropbox API.
It makes it simple to open up a connection and synchronize files between 
the local machine and a Dropbox account.
