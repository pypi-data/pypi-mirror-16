// Generated by CoffeeScript 1.10.0
(function() {
  $(document).ready(function() {
    var i, len, page, ref, url;
    console.log("Transitions Ready!");
    ref = $(".body-page");
    for (i = 0, len = ref.length; i < len; i++) {
      page = ref[i];
      url = $(page).data("load");
      $(page).load("" + url);
    }
    return $(document).on("click", "a, button", function(event) {
      var show_, target;
      if (!$(this).hasClass("no-outpage")) {
        event.preventDefault();
        url = $(this).data("load");
        if (!url) {
          url = $(this).attr("data-load");
        }
        target = $(".body[data-load='" + url + "']");
        $(".body.body-current").css({
          "-webkit-transition-duration": "0.1s"
        });
        $(".body.body-current").css({
          "-webkit-transform": "translateY(100vh)"
        });
        $(".body").removeClass("body-current");
        show_ = function() {
          target.addClass("body-current");
          target.css({
            "-webkit-transition-duration": "0.1s"
          });
          return target.css({
            "-webkit-transform": "translateY(0)"
          });
        };
        return setTimeout(show_, 50);
      }
    });
  });

}).call(this);
